# K8s Assistant 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# ============= 数据库配置 =============
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/k8sassistant
REDIS_URL=redis://localhost:6379/0

# ============= 安全配置 =============
# SECRET_KEY 用于 JWT 签名，必须设置为安全的随机字符串
# 生成方式: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============= CORS 配置 =============
# ENVIRONMENT: 环境类型
#   - development: 开发环境，允许 localhost
#   - production: 生产环境，禁止 localhost，推荐 HTTPS
ENVIRONMENT=development

# CORS_ORIGINS: 允许的跨域源列表
# 支持两种格式：
#   1. JSON 数组格式: CORS_ORIGINS=["http://localhost:3000"]
#   2. 逗号分隔格式: CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# 开发环境示例（前端运行在 3000 端口）:
CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
# 生产环境示例（取消注释并修改为实际域名）:
# CORS_ORIGINS=["https://yourdomain.com","https://www.yourdomain.com"]

# CORS_ALLOW_CREDENTIALS: 是否允许携带凭证（cookies、authorization headers）
CORS_ALLOW_CREDENTIALS=true

# CORS_ALLOW_METHODS: 允许的 HTTP 方法
# JSON 数组格式: CORS_ALLOW_METHODS=["GET","POST","OPTIONS"]
CORS_ALLOW_METHODS=["GET","POST","OPTIONS"]

# CORS_ALLOW_HEADERS: 允许的请求头
# JSON 数组格式: CORS_ALLOW_HEADERS=["Content-Type","Authorization","X-Requested-With","Accept"]
CORS_ALLOW_HEADERS=["Content-Type","Authorization","X-Requested-With","Accept"]

# ============= LLM 配置 =============
LLM_API_KEY=your-openai-api-key
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-3.5-turbo

# ============= 嵌入模型配置 =============
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
EMBEDDING_DEVICE=cpu
EMBEDDING_LOCAL_DIR=/app/models
EMBEDDING_CACHE_DIR=/app/.cache/huggingface
HF_MIRROR_BASE_URL=https://huggingface.co
HF_OFFLINE=false

# ============= 其他配置 =============
DEBUG=False
LOG_LEVEL=INFO